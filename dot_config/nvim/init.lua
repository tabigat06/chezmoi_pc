-- ЗАМЕНИ этот файл (и всю папку) своим реальным конфигом nvim.
--
-- Если твой конфиг — это не один init.lua, а целая структура
-- (lua/plugins/*.lua, lazy-lock.json и т.д.), проще всего добавить
-- сразу всю папку целиком:
--
--   chezmoi add ~/.config/nvim
--
-- chezmoi рекурсивно заберёт все файлы из ~/.config/nvim в свой source-path,
-- сохранив структуру. После этого просто работай с nvim как обычно и
-- периодически chezmoi add изменённые файлы.
